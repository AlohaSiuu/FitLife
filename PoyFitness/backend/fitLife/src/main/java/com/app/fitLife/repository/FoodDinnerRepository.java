package com.app.fitLife.repository;

import com.app.fitLife.model.FoodDinner;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FoodDinnerRepository extends JpaRepository<FoodDinner, Long> {
}
